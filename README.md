EduBot

AI-агент для автоматизации подготовки уроков IT-School

Быстрый старт

```bash
git clone <repo>
cd edubot
cp .env.example .env
# Заполните .env ключами
pip install -r requirements.txt
uvicorn main:app --reload
